apiVersion: apps/v1
kind: Deployment
metadata:
  name: student-outcome-prediction-modeltext
spec:
  replicas: 2
  selector:
    matchLabels:
      app: student-outcome-prediction-modeltext
  template:
    metadata:
      labels:
        app: student-outcome-prediction-modeltext
    spec:
      containers:
      - name: spam-prediction
        image: student-outcome-prediction-modeltext
        ports:
        - containerPort: 8501
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"